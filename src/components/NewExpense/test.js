// import React, { useState } from "react";

import "./test.css";

// don't change the Component name "App"
export default function Test() {
  /*
  First Test
  const [verdict, setVerdict] = useState("Enter");

  const messageVerdict = (event) => {
    
        // setVerdict(event.target.value)
        const userInput = event.target.value
        // setUserInput(event.target.value)
        
        if (userInput.length >= 3) {
            setVerdict('Valid')
        } else {
            setVerdict('Invalid')
        }
    }
    
    return (
        <form>
            <label>Your message</label>
            <input type="text" onChange={messageVerdict}/>
            <p>{verdict} message</p>
        </form>
    );
    

    Second Test
  const [counter, setCounter] = useState(0);

  const incrementHandler = (ops) => {
    if (ops === "i") {
      const newValue = counter + 1;
      setCounter(newValue);
    //   console.log(counter)
    }
  };

  return (
    <div>
      <p id="counter">{counter}</p>

      <button onClick={() => incrementHandler("i")}>Increment</button>
    </div>
  );
  */
}
